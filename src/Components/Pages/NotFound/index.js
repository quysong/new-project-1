import React from 'react';
import PropTypes from 'prop-types';

const NotFound = props => {
    return (
        <div>
            page 404
        </div>
    );
};

NotFound.propTypes = {
    
};

export default NotFound;